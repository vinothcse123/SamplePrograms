#include<stdio.h>

int main()
{
   /* Excluding characters */

   char str[100];

   scanf("%[^a]s",str);

   puts(str);



   gets(str);
   puts(str);

   fgets(str,4,stdin);
   fputs(str,stdout);

   return 0;
}
